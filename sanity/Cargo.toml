[package]
name = "sanity"
version = "0.1.0"
edition = "2021"

[dependencies]
sanitise = { path = ".." }
clap = { version = "4.2", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
incremental = true
codegen-units = 16

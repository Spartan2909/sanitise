on-title: split
processes:
  - name: validate
    columns:
      - title: time
        column-type: integer
        null-surrogate: -1
        on-null: previous
        null-sentinel: 0
      - title: pulse
        column-type: integer
        null-surrogate: -1
        max: 100
        min: 40
        on-invalid: average
        valid-streak: 3
        on-null: previous
        null-sentinel: 0
      - title: movement
        column-type: integer
        null-surrogate: -1
        valid-values: [0, 1]
        output-type: boolean
        output: "value == 1"
        on-null: delete
  - name: process
    columns:
      - title: time
        column-type: integer
        output: "value / 1000"
      - title: pulse
        column-type: integer
        ignore: true
      - title: movement
        column-type: integer
        ignore: true
